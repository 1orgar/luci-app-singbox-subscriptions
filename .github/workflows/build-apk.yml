name: Build and Publish APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install OpenWrt SDK
        run: |
          # Пример для x86_64, замените под свою архитектуру если нужно
          wget https://downloads.openwrt.org/releases/24.10.4/targets/x86/64/openwrt-sdk-24.10.4-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst
          tar xf openwrt-sdk-24.10.4-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst --strip-components=1
          rm -f openwrt-sdk-24.10.4-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst

      - name: Setup Keys
        run: |
          echo "${{ secrets.APK_PRIVATE_KEY }}" > private.pem
          # Утилита для подписи ожидает ключ в определенном месте
          mkdir -p ~/.abuild
          cp private.pem ~/.abuild/saved-key.rsa

      - name: Build Package
        run: |
          PKG_NAME="luci-app-singbox-subscriptions"
          
          # 1. Удаляем старые попытки и создаем чистую структуру
          rm -rf package/$PKG_NAME
          mkdir -p package/$PKG_NAME
          
          # 2. Копируем файлы проекта напрямую в папку package SDK
          cp $GITHUB_WORKSPACE/Makefile package/$PKG_NAME/
          [ -d "$GITHUB_WORKSPACE/htdocs" ] && cp -r $GITHUB_WORKSPACE/htdocs package/$PKG_NAME/
          [ -d "$GITHUB_WORKSPACE/root" ] && cp -r $GITHUB_WORKSPACE/root package/$PKG_NAME/
          
          # 3. Принудительно устанавливаем права на скрипт
          if [ -d "package/$PKG_NAME/root/usr/bin" ]; then
            chmod +x package/$PKG_NAME/root/usr/bin/*
          fi

          # 4. Обновляем фиды
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          
          # 5. КЛЮЧЕВОЙ МОМЕНТ: пересобираем индекс пакетов, чтобы SDK увидел новичка
          make package/symlinks
          
          # 6. Включаем пакет в конфиг
          echo "CONFIG_PACKAGE_$PKG_NAME=y" >> .config
          make defconfig
          
          # 7. Сборка (используем упрощенный вызов)
          make package/$PKG_NAME/compile V=s


      - name: Prepare Repository Structure
        run: |
          mkdir -p deploy/x86_64
          cp bin/packages/x86_64/base/*.apk deploy/x86_64/
          cd deploy/x86_64
          
          # Создаем индекс и подписываем его
          # (Используем контейнер alpine для доступа к инструментам apk)
          docker run --rm -v $(pwd):/work -w /work alpine:latest sh -c "
            apk add apk-tools abuild
            apk index -o APKINDEX.tar.gz *.apk
            abuild-sign APKINDEX.tar.gz
          "

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy
          publish_branch: repo
